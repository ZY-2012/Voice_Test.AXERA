# 音频测试数据清单（速查）

> Axera 音频 benchmark（Voice_Test.AXERA）评测数据目录/数量/下载地址速查；**制作方法与已知限制见工程内 `tools/数据总结.md`**。
> 所有数据不上传 GitHub；本清单所有路径均为**相对于数据根目录的相对路径**（数据根目录由 `configs/benchmark.yaml` 的 `paths.data_root` 配置）。
> 评测统一用 `benchmark/` 下的标准子集（确定性抽样，可复现）；原始全量数据保留在各模块目录。

---

## 1. ASR 语音识别（asr/）

| 数据集 | 语言/内容 | 数量 | 时长 | 本地地址 | 下载地址 |
|---|---|---|---|---|---|
| librispeech | 英文·有声书朗读 | 5,559 条 | 10.8h | `asr/librispeech/` | `http://www.openslr.org/resources/12/test-clean.tar.gz`、`test-other.tar.gz` |
| voxpopuli | 英文·欧洲议会 | 1,842 条 | 4.9h | `asr/voxpopuli/` | `https://huggingface.co/datasets/polinaeterna/voxpopuli`（test parquet，国内 hf-mirror.com）|
| tedlium | 英文·TED 演讲 | 1,142 条 | 2.6h | `asr/tedlium/` | `https://huggingface.co/datasets/AudioLLMs/tedlium3_test` |
| earnings22 | 英文多口音·财报电话 | 2,741 条 | 5.4h | `asr/earnings22/` | `https://huggingface.co/datasets/anton-l/earnings22_baseline_5_gram` |
| ami | 英文·多人会议 | 12,643 条 | 8.7h | `asr/ami/` | `https://huggingface.co/datasets/speech-seq2seq/ami` |
| gigaspeech | 英文·播客/YouTube | 19,931 条 | 35.4h | `asr/gigaspeech/`（含噪声段，暂未启用）| `https://huggingface.co/datasets/speechcolab/gigaspeech`（gated 需同意条款）|
| aishell1 | 中文·朗读 | 7,176 条 | 20.2h | `asr/aishell1/` | `https://www.modelscope.cn/datasets/modelscope/speech_asr_aishell1_testsets` |

统一格式：`wav/<utt_id>.wav`（16kHz PCM16）+ `wav.scp` + `text`（中文另有 `text.nospace` 字级版）。

## 2. VAD 语音活动检测（vad/）

| 数据集 | 内容 | 数量 | 本地地址 | 下载/生成 |
|---|---|---|---|---|
| picovoice | 2620 句拼接长流 + DEMAND 0dB 混噪；32ms 帧三值标签 | 19.9h / 224 万帧 | `vad/picovoice/` | 本地生成（源：LibriSpeech test-clean + DEMAND，`gen_picovoice_test.py`）|
| librivad | 采样级 0/1 标签（强制对齐）| 2,620 条 | `vad/librivad/test/` | `https://huggingface.co/datasets/LibriVAD/LibriVAD`（对齐文件）+ 本地生成 |
| aishell1-vad | 中文采样级 0/1 标签（funasr fsmn-vad 模型生成）| 7,176 条 | `vad/aishell1/test/` | 本地生成（`gen_aishell_vad_labels.py`）|
| tenvad | TEN VAD 官方 30 条 16k 音频 + scv 段标注 → 采样级 0/1 | 30 条 / 4.4min | `vad/tenvad/test/` | **零下载**：随模型仓库发布（`<model_root>/ten-vad/testset/`），`gen_tenvad_test.py` 转换 |
| demand | DEMAND 18 环境噪声（混噪用）| 5.2GB | `vad/demand/` | `https://www.kaggle.com/datasets/aanhari/demand-dataset` |

## 3. SE 语音增强（se/）

| 数据集 | 内容 | 数量 | 本地地址 | 下载地址 |
|---|---|---|---|---|
| voicebank_demand | clean+noisy 成对测试（40 种噪声条件）| 824 对 | `se/voicebank_demand/` | `https://huggingface.co/datasets/JacobLinCool/VoiceBank-DEMAND-16k` |

## 4. TTS 语音合成（tts/）

| 数据集 | 语言 | 数量 | 采样率 | 本地地址 | 下载地址 |
|---|---|---|---|---|---|
| ljspeech | 英文（单说话人）| 500 条（全量 13,100 条/24h）| 22.05k | `tts/ljspeech/` | `https://huggingface.co/datasets/flexthink/ljspeech`（test.txt 清单 + wav）|
| aishell3 | 中文多说话人 | 1,948 条 / 100 说话人（官方 test 24,773）| 44.1k | `tts/aishell3/` | `https://huggingface.co/datasets/AISHELL/AISHELL-3` |
| librispeech ✅ | 英文**多说话人** | 2,620 条 / 40 说话人（test-clean）| 16k | `tts/librispeech/` | **零下载**：复用 `asr/librispeech/`，按 `Voice_Test.AXERA/tts/librispeech_testclean_spk.txt`（40 个说话人 ID）过滤出 test-clean |
| zh_hardcase ✅ | 中文难例 | 150 句（纯文本，无音频）| — | `tts/zh_hardcase/`（`text` + `text.ref`）| **随仓库版本化**：定义在 `Voice_Test.AXERA/tts/zh_hardcase.tsv`，`prepare_tts.py` 物化 |

- **librispeech**：`asr/librispeech/` 混放 test-clean(2620)+test-other(2939)，原 tar 与 SPEAKERS.TXT 已不在盘；说话人清单由 `vad/librivad`（仅覆盖 test-clean）反查并交叉验证（补集恰为 2939 = test-other 官方条数）。⚠️ 转写为全大写无标点，需规范文本时用 ljspeech。
- **zh_hardcase**：绕口令 40 / 长句>40字 40 / 数字读法 35 / 同音叠字 35。数字类 `text`（输入 `3.8%`）与 `text.ref`（参考「百分之三点八」）不同 —— ASR 转写为汉字，用书面形式比对会全错。无参考音频 ⇒ 不适用 MCD/GT 锚点。判读口径见 `tts/README.md` §4.6。

TTS **客观评测模型**（host 侧，📋 规划，非数据集）：英文 ASR `openai/whisper-large-v3`、中文 ASR `funasr/paraformer-zh`、说话人相似度 `wavlm_large_finetune.pth`（[MS UniSpeech release](https://github.com/microsoft/UniSpeech)）、可选 MOS `pip install utmosv2`。选型理由见工程 `tts/README.md` §4.2，完整计划见 `tts/PLAN_objective_eval.md`。

---

## 5. 评测标准子集（benchmark/）

评测默认跑标准子集（确定性抽样 seed=20240824，`FULL=1` 可全量）：

| 模块 | 子集规模 | 位置 |
|---|---|---|
| ASR | 中文 200 / 英文各子集 100 | `benchmark/asr/<ds>/{ground_truth.txt, wav/, aishell_S0764/}` |
| VAD | librivad/aishell1 各 200 + tenvad 30（全量）；picovoice 长流不抽 | `benchmark/vad/<ds>/subset.scp` |
| SE | 200 对 | `benchmark/se/voicebank_demand/subset.scp` |
| TTS | 各 200 | `benchmark/tts/<ds>/{text, wav.scp}` |

## 6. 维护脚本

```bash
bash download_datasets.sh   # 下载（可选模块 asr/vad/se/tts）
bash prepare_datasets.sh    # 制作+抽子集
```
脚本位于工程 `Voice_Test.AXERA/` 根目录；详见工程内 `tools/数据总结.md`（含制作细节与已知限制：common_voice 源受限未纳入、gigaspeech 含噪声段、aishell3 为 HF 子集等）。
